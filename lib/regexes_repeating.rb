# Implement the below functions in order.
module RegexesRepeating
  # Returns a regex that matches a string that has "z" in it any number of
  # times (including zero times).
  def regex_matching_any_number_of_zs
    /WRITE ME/
  end

  # Returns a regex that matches a string that has "z" in it at least once.
  def regex_matching_at_least_one_z
    /WRITE ME/
  end

  # Returns a regex that matches a string that has "a", then a possible "z",
  # then "a".
  def regex_matching_z_at_most_once
    /WRITE ME/
  end

  # Returns a regex that matches a string that has "a", then exactly two "z"s,
  # then "a".
  def regex_with_exactly_two_zs
    /WRITE ME/
  end

  # Returns a regex that matches a string that has "a", then two or more "z"s,
  # then "a".
  def regex_with_two_or_more_zs
    /WRITE ME/
  end

  # Returns a regex that matches a string that has "a", then two to four "z"s,
  # then "a".
  def regex_with_two_to_four_zs
    /WRITE ME/
  end

  # Returns a regex that matches a string that has "a", then at most three "z"s,
  # then "a".
  def regex_with_at_most_three_zs
    /WRITE ME/
  end
end
